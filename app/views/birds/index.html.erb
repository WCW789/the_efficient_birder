<%= render partial: "shared/navbar" %>

<%= link_to "New bird", birds_photo_path %>
<br><br>

<h1>Birds</h1>

<%= link_to "Export Birding Journal", birds_export_path(format: :pdf) %>

<div id="birds">
  <% @birds.each do |bird| %>
    <%= render bird %>
  <% end %>
</div>
